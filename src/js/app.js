document.addEventListener("DOMContentLoaded", function() {
//This is just test code to ensure script file is connected delete this code once we get going
    const heading = document.querySelector("h1");
    const testMessage = document.querySelector(".testMessage");
    heading.textContent = "JavaScript file is connected!";
    testMessage.innerHTML = '<h2>Let the Games Begin!</h2>';


});